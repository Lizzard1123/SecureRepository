Code we should use, but im not sure how to use:
  
  turtle/rabbit
   if((Constants.fast > 0 && Constants.slow == 0) || (Constants.fast==0 && Constants.slow>0) || (Constants.fast==0 && Constants.slow==0) )
        {

            //speedy boi
            if((left<= Constants.maxSpeed && (left+Constants.fast)<=Constants.maxSpeed))
            { 
                    left = left + Constants.fast;
            }
            else if(left>= Constants.maxSpeed*-1 && (left-Constants.fast)*-1<=Constants.maxSpeed) 
            {
                left = left - Constants.fast;
            }
            if((right<= Constants.maxSpeed*-1 && (right+Constants.fast)*-1<=Constants.maxSpeed))
            {
                right = right + Constants.fast;
            }
            else if(
                (right >= Constants.maxSpeed && (right-Constants.fast)*-1 <= Constants.maxSpeed)
            )
            {
                right = right - Constants.fast;
            }
            
            //slow boi
            if(left>=0 && left-Constants.slow >=0 )
            {
                left = left - Constants.slow;
            }
            else if (left<=0 && left+Constants.slow<=0){
                left = left + Constants.slow;
            }
            if(right>=0 && right-Constants.slow>=0)
            {
                right = right - Constants.slow;
            }
            else if(right<=0 && right+Constants.slow<=0)
            {
                right = right + Constants.slow;
            }